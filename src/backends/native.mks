const init_backend = () => ();

const main_loop = (frame :: () -> ()) => (
    unwindable loop @loop (
        if raylib_WindowShouldClose() then (
            unwind loop with ();
        );
        frame();
    );
);
